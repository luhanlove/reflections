1、暂存区与工作目录和版本库有何不同？你认为它具有什么价值？
暂存区(staging area)相当一个打包缓冲，向暂存区发送的文件是可以撤销的，它相当于一个后悔药，一个预备役。
工作目录(working directory)本身是没有git功能的，要初始化之后才有
版本库(repository)是
git init初始化之后的，有git功能。
2.git diff 比较工作目录和暂存区之间的内容；
git diff --staged
比较暂存区和当前版本库之间的内容。
git reset --hard
删掉暂存区和工作目录中改动的部分。
git checkout master
